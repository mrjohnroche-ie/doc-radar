# Copy this to .env and paste your key in. .env is gitignored and never leaves
# this machine. Get a key at https://www.themoviedb.org/settings/api - either
# the short "API Key (v3 auth)" or the long "API Read Access Token" works.
TMDB_API_KEY=
